import 'babel-polyfill'
import { handleSubmit } from "../js/formHandler";

describe('Test client', () => {
    describe("handleSubimt", () => {
      test("is defined", () => {
        expect(handleSubmit).toBe();
      });
    });
});
